define([
    'underscore',
    'backbone',
    'thorax',
    'common'
], function(_, Backbone, Thorax, Common) {

    return Thorax.Model.extend({

        // Default attributes for the model
        defaults: {},

    });

});